# Footwear Development Module · 项目交接书

版本：交接 V1.8 ｜ 建立日期：2026-09-20 ｜ 当前阶段：V0.3 全量配色模块

**接手时先读本文件。当前已授权建立独立 V0.2 本地多款图片模块；用户可新增或移除款式，并在网页中直接上传鞋款图片。主站集成、正式 PDF/ZIP 与对外发布仍未授权自动执行。**

## 1. 项目目标与产品关系

在现有趋势网站之外，独立构建 Development Ready 女鞋开发模块；先用一款验证内容与视觉，满意并验收后，再嵌入现有离线 HTML 趋势网站。

最终产品是一个内容体系的两档订阅：趋势网站提供 Trend Intelligence；Pro 在同一份趋势内容后衔接 Design + Development。独立工作区只是开发隔离方式，最终不另建独立 SaaS、ERP 或鞋类 CAD 系统。

核心路径：趋势证据 → 买手判断 → 原创设计 → 开发资料 → 工厂工程开发与打样。Development Ready 表示资料足以让工厂理解设计并开展工程评估，不表示生产就绪、已确认可量产或无需工厂工作。

## 2. 商业模式与交付渠道

| 档位 | 示例年费 | 内容 |
| --- | --- | --- |
| Standard | ¥10,000/年 | Trend Intelligence：趋势图、案例、买手分析 |
| Pro | ¥16,000/年 | Standard 全部内容 + 精选 Development Ready 款：多视图、材料、BOM、配色、设计意图、开发建议 |

差价示例为 ¥6,000/年。以上是商业模式示例，不是已签约报价。每周趋势内容通过离线 HTML 供桌面阅读，PDF 供手机/微信阅读，是既有讨论中的交付方向；本次不发送邮件或微信。

开发款每期/每月数量、更新节奏、修改次数、定制服务、客户使用范围、最终报价均待确认。历史讨论中的每月 8–12 款等数字仅为建议，不写入承诺，也不承诺无限开发款或必然接到订单。

Standard 在构建时排除 Pro 的图、数据、BOM、PDF 和资产，不以 CSS/JavaScript 隐藏完整资料来区分权益。Pro 构建才包含审核通过的开发内容。

## 3. 职责与不可扩大的范围

| 我们负责 | 交付含义 |
| --- | --- |
| Trend | 来源明确的趋势证据与买手判断 |
| Design | Brief、候选设计、锁定母图与视觉语言 |
| Multi-view | 同一款的外侧、内侧、正面、俯视、后视及一致性复核 |
| Material | 材料方向、表面效果、配色与建议状态 |
| BOM | 使用统一部件编号的开发材料清单 |
| Design Intent | 结构标注、Design Lock 与允许调整范围 |
| Development Advice | 首版建议、成熟楦底跟匹配方向与待验证问题 |

工厂负责匹配成熟楦库、底库、跟库（MATCH LAST / MATCH SOLE / MATCH HEEL），完成纸版（PATTERN MAKING）、备料、内部结构工程、打样（SAMPLE MAKING），并验证合脚性、舒适性、成本及实际工艺。用户已说明合作工厂有成熟楦库和底库；具体可用库号、跟库和适配性仍由目标工厂确认。

明确不做：全新鞋楦设计/工程、全新鞋跟工程、全新大底工程、2D 纸版、CAD 生产文件、工厂内部结构工程、自动放码、生产级数字孪生。AI 多视图只是设计表达，不能标为工程 CAD。若必须新开楦/跟/底才能保留设计，应返回改款或记录范围外需求，不在 V1 内扩张。

## 4. 从设计到 Development Ready 的流程

1. Trend Opportunity：整理证据和 Design Brief，说明为什么做、给谁做、锁定哪些视觉特征。
2. Concept Design：探索有限候选，由用户选款；候选图保持 Concept 状态。
3. Master Design Lock：锁定外侧母图、款号、版本和 Design Lock，之后的视图引用同一母图。
4. Multi-view + QA：生成五个必需视角，检查鞋头、鞋口、跟、后带、五金、裁片、缝线的一致性。
5. Structure Map：统一部件编号，让标注、BOM、配色、工艺意图互相对应。
6. Material + Color + BOM：形成材料方向、首版配色与部件清单；未知参数写待确认。
7. Design Intent + Development Advice：明确不可改的设计特征、可调整工程空间和首版建议。
8. Development Ready Review：按下表逐项审核，记录证据与审核人；通过后进入 Pro 交付候选。

工厂接棒匹配楦底跟并打样；样鞋反馈与 Revision 可后续人工记录，不在本次建立自动化系统。

## 5. Development Ready Gate：12 项全部通过

| Gate | 最低通过证据 |
| --- | --- |
| TREND EVIDENCE | 有来源记录、相关趋势证据和买手解读 |
| DESIGN BRIEF | 品类、目标、设计机会、约束与锁定特征清楚 |
| MASTER DESIGN | 母图已由用户锁定，款号和版本明确 |
| MULTI-VIEW | 外侧、内侧、正面、俯视、后视齐备并可打开 |
| VIEW CONSISTENCY | 对照母图逐项复核通过，无未解决的关键视图冲突 |
| STRUCTURE MAP | 关键部件有唯一编号，标注与清单能对应 |
| MATERIAL | 各关键部位材料与表面方向明确，建议与确认值区分 |
| BOM | 关键部件齐全，材料/配色引用有效，无互相矛盾的条目 |
| COLORWAY | 首版配色明确，各部件颜色映射一致 |
| DESIGN LOCK | 必须保留的视觉特征及参考位置明确 |
| FACTORY ADJUSTABLE | 楦底跟匹配、内部结构、纸版等调整范围和回报条件明确 |
| DEVELOPMENT ADVICE | 首版路线、重点验证事项、工厂待确认项与责任人明确 |

状态约定：pending（待审核）、pass（通过）、fail（不通过）。12 项缺一不可，不用 N/A 凑齐。每项 pass 必须有证据、人工审核人、日期并对应当前款式版本。AI 可协助检查，不能自行充当最终签核人。

任一项 pending/fail、资料为演示数据、或存在阻断理解设计的未决问题，均不得显示 DEVELOPMENT READY 或进入正式 Pro 内容。全项通过且当前版本人工签核后才显示 READY FOR FACTORY DEVELOPMENT。Ready 仍不等于工程确认或可直接量产。

工厂具体库号、材料供应商等工程待确认项，只有在不妨碍理解设计且责任清楚时才可保留；关键设计歧义与缺失视图不能归为工厂待确认而绕过 Gate。母图或关键设计变更后，相关 Gate 重新审核。

## 6. 当前页面：纯图片上传与展示

2026-09-20 用户再次复核后，当前网页不再承载 Development 资料。它只负责上传和展示鞋款图片；工厂后续确有需求时，再单独评估材料、BOM、开发建议等内容。

| 层级 | 展示内容 |
| --- | --- |
| 默认卡片 | 一张固定大主图，桌面约占首行 60%；前三张视角放在主图旁，更多视角向下追加；所有配色以较小缩略图在卡片下方同时展开 |
| 多款管理 | 顶部显示款式总数，可用“新增款式”持续创建；新款排在第一页最前 |
| 分页 | 桌面每页四款，即两行 × 两列；超过四款自动分页，窄屏仍每页四款但单列排列 |
| 查看与上传 | 空图片格点击上传；已有图片点击放大查看，右上角“替换”负责重新上传，“删除”移除当前展示；也可拖图替换。支持 PNG、JPG、WebP、AVIF，单张不超过 12MB |
| 保存方式 | 图片写入本地项目资产目录，路径记录在 `data/uploaded-styles.json`，刷新后保留 |
| 移除方式 | 删除单图后，后续配色或视角自动前移并重新编号，不保留空格；移除款式或单图都只删除展示记录，已上传的原图片文件继续保留，避免误删素材 |

主页面不显示“AI 开发精选”、款式占位名称、款号、状态、品类标签、“查看开发细节”按钮或任何开发资料弹窗。可见图片标签保留“主图”和按顺序生成的“配色参考 01、02、03……”；视角图片位不显示正面、俯视、右侧等固定标签。

多视图、BOM、Structure Map、Design Lock、Gate 和签核文档只作为未来工厂资料工作的内部基础，当前上传展示页不读取、不显示，也不宣称完成 Development Ready。

## 7. 视觉原则

采用 **Sightline Editorial Grid + Uploadable Image Mosaic**：沿用主站黑白、细边框、无衬线字体和克制留白，参考用户手绘的“大主图 + 三个补充图 + 两个配色”关系，不复制参考站品牌、浮动工具条或其他功能。

桌面内容宽度达到 980px 时每行两款，低于该宽度每行一款；手机端单款内部改为主图、视角图组、配色图组的纵向组合。每页固定读取四个款式记录，款式数量增加后由页面分页承接。款式卡片内部不再使用图片翻页：主图在桌面固定约占首行 60%，新增配色不改变主图比例；前三张视角放在主图旁，更多视角和全部配色继续向下排列；配色在桌面按数量形成两列或三列，手机固定两列。配色名称位于图片外的窄底栏，不覆盖鞋款细节。空图片格负责上传；已有图片的主点击动作是大图预览，独立“替换”按钮才重新选择文件，“删除”移除并自动补位，不打开开发详情页面。

## 8. 数据驱动与 JSON

一套模板读取结构化 Style Data；换款通过换 JSON，不复制页面写死资料。顶层包括 identity、trend_origin、design_brief、master_design、multi_views、product_language、structure_map、materials、colorways、bom、design_lock、factory_adjustable、construction_intent、development_advice、assets、presentation、gate、review、revision_history。presentation 只是客户展示摘要，不替代内部完整数据。

当前网页的真实上传状态记录在 `data/uploaded-styles.json`，其中的 `order` 决定款式顺序；图片保存在 `prototype/assets/shoes/uploads/`。“新增款式”会生成新的 style-id 并持久化，当前本地实现设置 200 款技术保护上限，它不是业务上限或正式网站的容量承诺。`data/style-v0.1.js` 仅保留作未来工厂开发资料的数据草案，当前页面不读取它。若以后恢复工厂资料功能，再以 [数据契约](docs/04-data-schema.md) 为依据。

未知数值用 null，配 pending_confirmation 与说明；不能用 0、false 或虚构数字代替未知。例如“不建议新开模”是设计策略，不代表 new_tooling 已经确认 false。图片资产须有本地相对路径、来源与使用状态；只有 URL 不代表已保存文件。

## 9. 独立原型与后续集成

阶段顺序：V0.1 本地上传展示页 → V0.2 动态多款与分页 → V0.3 真实图片视觉验收 → V1 主站集成。工厂开发资料仅在用户后续明确提出时另开阶段。

从第一版即分离数据。V0.2 已验证五款数据下的两列、四款分页和新增/移除机制；正式后端、权限与线上存储仍待主站集成时确定。真实图片视觉验收优先，不以大量空卡替代真实内容完成。

采用独立根容器 development-module、dev- 前缀样式及局部脚本作用域；不修改主站全局 CSS。后续以主站 FINAL 的备份/隔离副本试集成，再接入其实际构建流程。入口、技术栈、字体资源与认证方式在读取最终基线后确定。

离线包使用本地资产和相对路径；直接打开 HTML 的数据读取策略须验证，不能假定 fetch 本地 JSON 必然可用。Standard / Pro 在构建阶段分别生成，HTML、PDF、ZIP 使用同一款号、版本和审核状态。

## 10. 2026-10-01 前的目标与验收

截止按北京时间 **2026-09-30 23:59** 理解；以下为计划目标，不是完成声明。

| 交付物 | 目标验收 | 当前状态 |
| --- | --- | --- |
| 趋势网站 FINAL | 用户确认最终基线，记录版本/备份及原有浏览路径 | 已核对 `fb484a7`；后续主站变更需重新基线化 |
| 图片展示模块 V1 | 上传真实鞋图，双栏图片展示通过验收后集成 | V0.3 已支持动态新增、四款分页、持久保存、单图删除补位与固定大主图；首款已导入真实图片，主站尚未集成 |
| PRO 离线 HTML | 解压后断网可打开，趋势与开发内容、图片和导航完整 | 未开始 |
| PDF 输出 | 同版内容，文字、图、表、标注可读，无裁切或遗漏 | 未开始 |
| 图片资产 ZIP | 所有本次应交付图片实际入包，有清单且逐项可打开 | 未开始 |
| 首个完整工厂演示包 | Standard/Pro 差异明确；至少 1 款完整真实资料、HTML、PDF、图片 ZIP、版本清单与阅读说明一致 | 未开始 |

建议排期：9/20 交接；9/21–22 确认主站 FINAL 参考与单款 V0.1；9/23–25 完成首款 V0.2 与 Gate；9/26–27 完成 V0.3、必要多款验证与隔离集成；9/28–29 输出与验收；9/30 留作修正和封包。排期依赖首款素材、人工审核和主站基线及时到位。若延期，优先保证单款真实完整，报告缺口，不降低 Gate 或宣称完成。

## 11. 当前完成边界与待确认

已完成：独立目录、7 份交接文档与内部数据草案；2026-09-20 核对主站 `main` 的 `fb484a7` 为当前干净基线；正式原型已改为桌面双栏的纯图片页面。每款固定一张大主图，视角和配色位置可动态增加；内部图片翻页已取消，所有配色同时展开，卡片随数量自动向下延伸。空位上传、已有图片大图预览、独立替换、单图删除自动补位、拖入替换和刷新保留均已建立。当前首款已有用户上传的真实主图和多张配色参考图，其余位置仍待补充。

未开始：首款真实图片上传、主站集成、PDF 和 ZIP 交付物。材料/BOM、12 项 Gate 和工厂开发资料暂不进入当前页面范围。页面无外部依赖，不需要安装套件。

待确认：首款真实趋势及设计输入；首款目标市场与品类；演示款总数；开发款供稿数量；最终商业条款；目标工厂库号与联系人；实际审核人；离线包和 PDF 阅读设备/版式；图片使用范围；最终演示包接收方式。

下一步：用户验收动态配色/视角版式；通过后进入批量智能导入阶段，先完成多文件选择、自动归位预览与人工确认，再决定是否接入视觉 AI 服务。此交接不授权自动发送资料、改主站或发布。

## 12. 文档入口与依据

- [README](README.md)：目录、阅读与使用方式。
- [产品定义](docs/01-product-definition.md)：用户、商业与职责边界。
- [开发流程](docs/02-development-workflow.md)：8 步流程和 Gate 操作规则。
- [页面信息架构](docs/03-page-information-architecture.md)：双栏图片拼图、本地上传与响应式规则。
- [数据契约](docs/04-data-schema.md)：字段、JSON 与状态规则。
- [集成计划](docs/05-integration-plan.md)：分期、离线输出和交付验收。

依据：用户 2026-09-20 当前任务和已读取的《女鞋开发流程分析》（会话 ID：6aaf480c-b870-83ea-9fe6-a19a15404f69）。以本次明确范围为准，早期全工程化设想不纳入 V1。既有趋势项目历史记录仅作为“继承既有布局、复验核心浏览”的提醒，不把旧包状态视为本次 FINAL。上述实现细节、字段和排期是交接建议；未经确认的商业和工厂事实均标为待确认。
